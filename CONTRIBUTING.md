# Contributing

Use a branch and pull request. Direct `main` pushes are not allowed after ruleset activation.

Every PR must include:
- bounded stage ID or issue reference;
- receipts or tests for changed behavior;
- replay or boot proof when runtime/export logic changes;
- security review when workflows, dependencies, or write paths change.
