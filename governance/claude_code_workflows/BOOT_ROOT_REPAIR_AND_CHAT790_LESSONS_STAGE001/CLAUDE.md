# Claude Code instructions for BOOT_ROOT_REPAIR_AND_CHAT790_LESSONS_STAGE001

Read `WORKFLOW_CONTRACT.json` first.

This stage activates the merged tool-routing and repeated-blocker guard scripts as Claude Code
repo hooks. The deliverable is `.claude/settings.json` wiring PreToolUse and PostToolUseFailure
to the guard scripts, plus subprocess-based tests proving the hook path works end-to-end.

Do NOT:
- use file_search for mounted /mnt/data OS artifact truth
- claim native MetaBlooms OS source was patched in this repo
- start the chat_export_mining cartridge
- proceed beyond one bounded stage
