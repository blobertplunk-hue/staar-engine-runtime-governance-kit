# Stage0Q Post-Merge Main Manifest Trigger

Purpose: create a PR-visible workflow run after PR #19 merged so the manifest workflow can explicitly check out `main` and prove the checked-out commit in the artifact.

No OS/GitHub file movement is authorized by this trigger file.
