# STAGE0V_PR_VISIBLE_MANIFEST_TRIGGER

Purpose: open a PR-visible manifest trigger. The repository workflow checks out `main` for pull_request events, so this PR should cause a connector-visible run while generating a manifest from the current `main` checkout.

This is a trigger-only governance receipt.
